extends external

block mainContent
	#error
		p Is this your card?
		.errorCard.visible.showValue
			.cardInner
				.cardValue #{status}
				.cornerValue.topleft #{status}
				.cornerValue.bottomright #{status}
