extends layout

block content
	#voterRoomInfo Room:
		#roomName #{room.roomId}
		#voterList Players:&nbsp;
			.avatars
	#voterInfo
		.status.newRound
			| Voting is open - choose a card
			span#ticketInfo
		.status.roundEnd Voting is closed
		
		table#estimateOptions.roundEnd

		#cardStyle
			h4 Card Style
			div#cardPreview
				div.cardBack.voter.argyle(style='background-color:#{user.cardColor}')


	script.
		BP.room = !{JSON.stringify(room)};
		BP.user = !{JSON.stringify(user)};
	script(src="/socket.io/socket.io.js")
	script(src="/javascripts/join.js")
